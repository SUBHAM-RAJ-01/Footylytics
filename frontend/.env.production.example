# Frontend Production Environment Variables
# Copy this to .env.production and fill in your actual values
# DO NOT commit the actual .env.production file to GitHub!

# Backend API URL (from Railway/Render)
VITE_API_URL=https://your-backend-url.up.railway.app/api

# Supabase
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Stripe (LIVE keys for production!)
VITE_STRIPE_PUBLIC_KEY=pk_live_51...

# Notes:
# - Use LIVE Stripe keys (pk_live_...) not test keys
# - Get backend URL after deploying to Railway/Render
# - Supabase anon key is safe to expose (it's public)
# - Never use service role key in frontend!
